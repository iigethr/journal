/* Aside Navigation */

.aside-navigation
  .aside-navigation-box
    .aside-navigation-item
      = link_to "toggle", "#", class: "altai-off toggle-btn", class: "altai-off toggle-btn link blue #{active_class(controller: "publications", action: "show")}"
    .aside-navigation-item
      = link_to "Preview", preview_publication_article_path(@publication, article), class: "link blue #{active_class(controller: "publications", action: "show")}"

.aside-navigation
  .aside-navigation-box
    .aside-navigation-item
      = link_to "Text", new_publication_article_text_path(@publication, article), class: "link blue #{active_class(controller: "publications", action: "show")}"
    .aside-navigation-item
      = link_to "Image", new_publication_article_picture_path(@publication, article), class: "link blue #{active_class(controller: "publications", action: "show")}"
