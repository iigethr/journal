= link_to "back", publication_articles_path(@article.publication.slug)

%p
  %b Publication:
  = @article.publication.title
%p
  %b Title:
  = @article.title
%p
  %b Description:
  = @article.description
%p
  %b Published:
  = @article.published

- if params[:publication_id]
  = link_to "Edit", edit_publication_article_path(@publication, @article)
  \|
  = link_to "Back", publication_articles_path(@publication)

%ul
  - @agents.each do |item|
    = render_agent(@article, item)

= link_to "Text", new_article_text_path(@article)
= link_to "Image", new_article_picture_path(@article)
