%p
  %b Publication:
  = @article.publication
%p
  %b Title:
  = @article.title
%p
  %b Description:
  = @article.description
%p
  %b Published:
  = @article.published
%p
  %b Position:
  = @article.position

- if params[:publication_id]
  = link_to "Edit", edit_publication_article_path(@publication, @article)
  \|
  = link_to "Back", publication_articles_path(@publication)

%ul
  - @agents.each do |item|
    = render_agent(item)

= link_to "Image", new_article_picture_path(@article)
