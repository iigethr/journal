/* Aside Navigation */

.aside-navigation
  .aside-navigation-box
    .aside-navigation-item
      = link_to "toggle", "#", class: "altai-off toggle-btn", class: "altai-off toggle-btn link blue #{active_class(controller: "publications", action: "show")}"
    .aside-navigation-item
      = link_to "Preview", preview_section_path(section), class: "link blue #{active_class(controller: "publications", action: "show")}"

.aside-navigation
  .aside-navigation-box
    .aside-navigation-item
      = link_to "Text", new_section_text_path(section), class: "link blue #{active_class(controller: "publications", action: "show")}"
    .aside-navigation-item
      = link_to "Image", new_section_picture_path(section), class: "link blue #{active_class(controller: "publications", action: "show")}"
