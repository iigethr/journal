/* Form */

= form_with model: [parent, piece], local: true do |form|
  = errors(piece)
  %fieldset
    .form-row
      %legend Text
    .form-row
      .field
        = form.label :body
        .textarea
          = form.rich_text_area :body
    = submit_button(form, "Save")
