/* Session */

.session
  .session-box
    .session-item
      = link_to profile_path do
        = current_user.profile.name
    .session-item
      = link_to destroy_user_session_path, method: :delete do
        Logout
